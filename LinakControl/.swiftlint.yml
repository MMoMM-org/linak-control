disabled_rules:
  - trailing_whitespace

opt_in_rules:
  - empty_count
  - explicit_init

line_length:
  warning: 120
  error: 200
  ignores_comments: true
  ignores_urls: true

excluded:
  - .build
  - Tests
